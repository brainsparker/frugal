{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-12-11/server.schema.json",
  "name": "io.github.brainsparker/frugal",
  "title": "Frugal",
  "description": "The open routing layer for AI tools: an MCP server that routes agent tool calls across providers per policy — cost (default), latency, pinning, deny lists — with automatic failover and the routing decision on every result.",
  "repository": {
    "url": "https://github.com/brainsparker/frugal",
    "source": "github"
  },
  "version": "0.0.0",
  "packages": [
    {
      "registryType": "npm",
      "identifier": "frugal-mcp",
      "version": "0.0.0",
      "runtimeHint": "npx",
      "packageArguments": [
        {
          "type": "positional",
          "value": "mcp"
        },
        {
          "type": "positional",
          "value": "serve"
        }
      ],
      "transport": {
        "type": "stdio"
      }
    }
  ]
}
